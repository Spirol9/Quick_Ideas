package me.deveon.quick_ideas.shared_damage.main;import java.util.logging.Level;import me.deveon.quick_ideas.main.Main;import me.deveon.quick_ideas.shared_damage.events.onPlayerDamged;import org.bukkit.Bukkit;import org.bukkit.event.HandlerList;import org.bukkit.event.Listener;public class SDMain {  private Main instance;  private static Listener[] listeners;  public SDMain(Main main) {    instance = main;    register();  }  private void register() {    // Events //    listeners = new Listener[]{        new onPlayerDamged(instance)    };    // registers listeners, to add a new listener please go up and add it from there    Listener[] ArrayOfListeners;    int a = (ArrayOfListeners = listeners).length;    for (int i = 0; i < a; i++) {      Bukkit.getServer().getPluginManager().registerEvents(ArrayOfListeners[i], instance);    }  }  public static Listener[] getListeners() {    return listeners;  }  public void disableListeners() {    Listener[] ArrayOfListeners;    int a = (ArrayOfListeners = listeners).length;    for (int i = 0; i < a; i++) {      HandlerList.unregisterAll(ArrayOfListeners[i]);    }  }}